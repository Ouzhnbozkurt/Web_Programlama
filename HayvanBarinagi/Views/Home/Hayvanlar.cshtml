@using HayvanBarinagi.Models
@model List<Hayvan>
<div>
    <table class="table">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Adi</th>
      <th scope="col">Yas</th>
      <th scope="col">Cins</th>
    </tr>
  </thead>
  <tbody>
      @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Adi</td>
                    <td>@item.Yas</td>
                    <td>@item.Cins</td>
                </tr>
            }
        </tbody>
</table>
</div>